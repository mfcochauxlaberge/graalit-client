class JSONAPIFetcher {
  base = ''

  getOne = ({ type, id, fields, include }) => {
  }

  getMany = ({ type, fields, filter, sort, pageSize, pageNumber, include }) => {
  }

  post = ({ type, data, include }) => {
  }

  patch = ({ type, id, updates }) => {
  }

  delete = ({ type, id }) => {
  }
}

export { JSONAPIFetcher }